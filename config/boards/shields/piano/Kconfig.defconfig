if SHIELD_PIANO_DONGLE

config ZMK_KEYBOARD_NAME
	default "The Piano"

config ZMK_SPLIT_ROLE_CENTRAL
	default y

config ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS
	default 2

endif

if SHIELD_PIANO_DONGLE || SHIELD_PIANO_LEFT || SHIELD_PIANO_RIGHT

config ZMK_SPLIT
	default y

endif
